function projectManifest(){const s=projectScale();return{app:'ColorLayer',version:'0.3.1',artwork:state.filenameBase,processedPixels:{width:state.w,height:state.h},physical:{width_mm:s.width,height_mm:s.height,layer_thickness_mm:positive(els.layerMm.value,.6),backing_thickness_mm:positive(els.backingMm.value,2),frame_width_mm:positive(els.frameMm.value,8)},registration:{mode:els.registrationMode.value,diameter_mm:positive(els.pinDiameter.value,3),margin_mm:positive(els.pinMargin.value,6)},background_index:state.backgroundIndex,palette:state.palette.map((x,i)=>({index:i+1,name:x.name,hex:rgbToHex(x.rgb),filament:x.filament,visible:x.visible,background:i===state.backgroundIndex}))}}
function saveProjectObject(){return{...projectManifest(),sourceDataUrl:state.sourceDataUrl,indices:Array.from(state.indices||[]),paletteRaw:state.palette,w:state.w,h:state.h,filenameBase:state.filenameBase,ui:{colorCount:els.colorCount.value,simplify:els.simplify.value,minIsland:els.minIsland.value,vectorTolerance:els.vectorTolerance.value,excludeBackground:els.excludeBackground.checked,transparentBg:els.transparentBg.checked}}}
els.saveProjectBtn.onclick=()=>downloadBlob(new Blob([JSON.stringify(saveProjectObject())],{type:'application/json'}),`${safe(state.filenameBase)}_ColorLayer_project.json`,'application/json');
els.openProjectInput.onchange=e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const p=JSON.parse(r.result);restoreProject(p)}catch(err){alert('Could not open ColorLayer project: '+err.message)}};r.readAsText(f)};
function restoreProject(p){if(!p.sourceDataUrl||!p.indices||!p.paletteRaw)throw new Error('Missing project data');state.sourceDataUrl=p.sourceDataUrl;state.filenameBase=p.filenameBase||'artwork';const img=new Image();img.onload=()=>{state.sourceImage=img;state.w=p.w;state.h=p.h;for(const c of[els.sourceCanvas,els.quantCanvas]){c.width=p.w;c.height=p.h}els.sourceCanvas.getContext('2d').drawImage(img,0,0,p.w,p.h);state.indices=Uint16Array.from(p.indices);state.palette=p.paletteRaw;state.backgroundIndex=p.background_index??null;if(p.physical){els.widthMm.value=p.physical.width_mm;els.layerMm.value=p.physical.layer_thickness_mm;els.backingMm.value=p.physical.backing_thickness_mm;els.frameMm.value=p.physical.frame_width_mm}if(p.registration){els.registrationMode.value=p.registration.mode;els.pinDiameter.value=p.registration.diameter_mm;els.pinMargin.value=p.registration.margin_mm}if(p.ui){els.colorCount.value=p.ui.colorCount;els.simplify.value=p.ui.simplify;els.minIsland.value=p.ui.minIsland;els.vectorTolerance.value=p.ui.vectorTolerance;els.excludeBackground.checked=p.ui.excludeBackground;els.transparentBg.checked=p.ui.transparentBg}els.colorCount.oninput();els.simplify.oninput();els.minIsland.oninput();els.vectorTolerance.oninput();els.processBtn.disabled=false;els.exportProjectBtn.disabled=false;els.saveProjectBtn.disabled=false;renderAll();els.status.textContent='Project restored'};img.src=p.sourceDataUrl}

els.exportProjectBtn.onclick=async()=>{if(!state.indices)return;els.exportProjectBtn.disabled=true;els.status.textContent='Building manufacturing ZIP…';await tick();const files=[];files.push({name:'manifest.json',data:new TextEncoder().encode(JSON.stringify(projectManifest(),null,2))});for(let i=0;i<state.palette.length;i++){if(!state.palette[i].visible)continue;if(i===state.backgroundIndex&&els.excludeBackground.checked)continue;files.push({name:fileName(i,'svg'),data:new TextEncoder().encode(svgString(i))});files.push({name:fileName(i,'dxf'),data:new TextEncoder().encode(dxfString(i))});const stl=new Uint8Array(await layerSTLBlob(i).arrayBuffer());files.push({name:fileName(i,'stl'),data:stl})}files.push({name:'BACKING.stl',data:new Uint8Array(await backingSTLBlob().arrayBuffer())});files.push({name:'FRAME.stl',data:new Uint8Array(await frameSTLBlob().arrayBuffer())});files.push({name:'ASSEMBLY.3mf',data:new Uint8Array(await assembly3MFBlob().arrayBuffer())});files.push({name:'README.txt',data:new TextEncoder().encode(exportReadme())});const zip=makeZip(files);downloadBlob(zip,`${safe(state.filenameBase)}_ColorLayer_manufacturing.zip`,'application/zip');els.exportProjectBtn.disabled=false;els.status.textContent=`Manufacturing ZIP exported (${files.length} files)`};
function exportReadme(){const s=projectScale();return`ColorLayer V0.3.1 manufacturing export\n\nArtwork: ${state.filenameBase}\nPhysical size: ${s.width.toFixed(2)} x ${s.height.toFixed(2)} mm\nLayer thickness: ${positive(els.layerMm.value,.6)} mm\nBacking: ${positive(els.backingMm.value,2)} mm\nFrame width: ${positive(els.frameMm.value,8)} mm\n\nFiles:\n- L##_*.svg: closed simplified vector contours\n- L##_*.dxf: CAD polylines in millimeters\n- L##_*.stl: experimental direct extrusions from the raster mask\n- BACKING.stl: rectangular backing with optional registration pins\n- FRAME.stl: four-piece perimeter frame shell\n- ASSEMBLY.3mf: combined colored layer assembly for 3MF-capable slicers\n- manifest.json: color/filament/manufacturing metadata\n\nImportant V0.3 note:\nThe STL generator is intentionally conservative and raster-derived. SVG/DXF are the preferred geometry for final CAD cleanup and high-quality parametric assembly work. Isolated artwork islands do not automatically receive physical breakaway carriers yet.\n`}

function makeZip(files,mime='application/zip'){const enc=new TextEncoder();let offset=0,locals=[],centrals=[];for(const f of files){const name=enc.encode(f.name),data=f.data instanceof Uint8Array?f.data:new Uint8Array(f.data),crc=crc32(data);const local=new Uint8Array(30+name.length+data.length),dv=new DataView(local.buffer);dv.setUint32(0,0x04034b50,true);dv.setUint16(4,20,true);dv.setUint16(6,0,true);dv.setUint16(8,0,true);dv.setUint16(10,0,true);dv.setUint16(12,0,true);dv.setUint32(14,crc,true);dv.setUint32(18,data.length,true);dv.setUint32(22,data.length,true);dv.setUint16(26,name.length,true);dv.setUint16(28,0,true);local.set(name,30);local.set(data,30+name.length);locals.push(local);const central=new Uint8Array(46+name.length),cv=new DataView(central.buffer);cv.setUint32(0,0x02014b50,true);cv.setUint16(4,20,true);cv.setUint16(6,20,true);cv.setUint16(8,0,true);cv.setUint16(10,0,true);cv.setUint16(12,0,true);cv.setUint16(14,0,true);cv.setUint32(16,crc,true);cv.setUint32(20,data.length,true);cv.setUint32(24,data.length,true);cv.setUint16(28,name.length,true);cv.setUint16(30,0,true);cv.setUint16(32,0,true);cv.setUint16(34,0,true);cv.setUint16(36,0,true);cv.setUint32(38,0,true);cv.setUint32(42,offset,true);central.set(name,46);centrals.push(central);offset+=local.length}const centralSize=centrals.reduce((a,b)=>a+b.length,0),end=new Uint8Array(22),ev=new DataView(end.buffer);ev.setUint32(0,0x06054b50,true);ev.setUint16(4,0,true);ev.setUint16(6,0,true);ev.setUint16(8,files.length,true);ev.setUint16(10,files.length,true);ev.setUint32(12,centralSize,true);ev.setUint32(16,offset,true);ev.setUint16(20,0,true);return new Blob([...locals,...centrals,end],{type:mime})}
let crcTable=null;function crc32(data){if(!crcTable){crcTable=new Uint32Array(256);for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=(c&1)?0xedb88320^(c>>>1):c>>>1;crcTable[n]=c>>>0}}let c=0xffffffff;for(const b of data)c=crcTable[(c^b)&255]^(c>>>8);return(c^0xffffffff)>>>0}

function fileName(i,ext){const l=state.palette[i],hex=rgbToHex(l.rgb).slice(1);return`L${String(i+1).padStart(2,'0')}_${safe(l.name||'layer')}_${hex}.${ext}`}
function safe(s){return String(s).trim().replace(/[^a-z0-9_-]+/gi,'_').replace(/^_+|_+$/g,'')||'file'}
function downloadBlob(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1500)}
function rgbToHex(c){return'#'+c.map(v=>Math.max(0,Math.min(255,Math.round(v))).toString(16).padStart(2,'0')).join('').toUpperCase()}
function hexToRgb(h){h=h.replace('#','');return[parseInt(h.slice(0,2),16),parseInt(h.slice(2,4),16),parseInt(h.slice(4,6),16)]}
function positive(v,d){v=Number(v);return Number.isFinite(v)&&v>=0?v:d}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function escapeAttr(s){return escapeHtml(s)}

